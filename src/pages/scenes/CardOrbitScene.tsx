import ScenePage from '../common/ScenePage';
import { SCENE_TITLES, SCENE_URLS } from './const';

const CardOrbitScene = () => {
  return (
    <ScenePage sceneUrl={SCENE_URLS.cardOrbit} title={SCENE_TITLES.cardOrbit} />
  );
};

export default CardOrbitScene;
